# Application URL (for SEO and OGP)
NEXT_PUBLIC_APP_URL=https://monocollector.com

# PostgreSQL Database URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:password@localhost:5432/mono_collector"

# NextAuth.js Configuration
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# Auth URL (required for custom domains)
# Set this to your production domain
AUTH_URL="https://monocollector.com"

# Google OAuth (for Google login)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# IMPORTANT: Add the following redirect URI in Google Cloud Console:
#   https://monocollector.com/api/auth/callback/google
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Optional: OpenAI API Key (for advanced image recognition)
# OPENAI_API_KEY=sk-xxx

# Optional: Web Push通知 (VAPID Keys)
# 未設定の場合はデプロイ時に自動生成されDBに保存されます
# 手動で設定する場合: npx web-push generate-vapid-keys
# NEXT_PUBLIC_VAPID_PUBLIC_KEY="your-vapid-public-key"
# VAPID_PRIVATE_KEY="your-vapid-private-key"

# Optional: Cron Job認証シークレット（Vercel Cronの場合は自動認証）
# 外部cronサービスを使う場合のみ設定
# CRON_SECRET="your-cron-secret"

# Optional: Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
